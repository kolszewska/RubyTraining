<%= form_with(model: @feedback,local: true) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_area :content, placeholder: "Create a new feedback..." %>
  </div>
  <%= f.submit "Post", class: "btn btn-primary" %>
<% end %>
